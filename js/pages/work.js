/* =========================================================
   WORK PAGE
   Keep work-page interactions isolated from Home animations.
========================================================= */

document.addEventListener("DOMContentLoaded", () => {
    // Category filtering and project mockup interactions will live here.
});
